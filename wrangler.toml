name = "mahabharatadecode"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[vars]
BREVO_LIST_ID = "2"

# NOTE: the SUBS KV binding (namespace "mbd-subscriptions",
# id 43528b88da1a476e8c8b476c4b6cf7df) is configured in the Cloudflare
# Pages dashboard (Settings → Functions → KV namespace bindings), not here —
# Git-connected Pages builds don't apply wrangler.toml resource bindings.
