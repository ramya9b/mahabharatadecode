name = "mahabharatadecode"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[vars]
BREVO_LIST_ID = "2"

# Server-of-record for paid subscriptions. This project is configured via
# this file (Pages configuration), so bindings live here — the dashboard's
# "Add binding" is intentionally disabled when a config file is present.
# Namespace: mbd-subscriptions.
[[kv_namespaces]]
binding = "SUBS"
id = "43528b88da1a476e8c8b476c4b6cf7df"
